include:
  - project: 'gitlab/templates/containers'
    ref: main
    file: '/default.yml'


test-with-password:
  stage: test
  needs:
    - build
    - test
  script:
    - docker pull "${CI_IMAGE}"
    - docker run --rm -e CI=true -e REDIS_PASSWORD=testpass "${CI_IMAGE}"
